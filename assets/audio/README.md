# Background music

ใส่ไฟล์เพลง MP3 ที่ต้องการไว้ที่:

- `assets/audio/background/main.mp3`

ระบบจะเล่นไฟล์นี้อัตโนมัติหลังจากผู้ใช้กดปุ่ม `เล่นเพลง` ใน popup บังคับหน้าแรก

ถ้าต้องการเปลี่ยนชื่อไฟล์/ตำแหน่ง ให้แก้ค่าคงที่ `DEFAULT_AUDIO_PATH` ในไฟล์:

- `scripts/forcedMusicPrompt.js`
